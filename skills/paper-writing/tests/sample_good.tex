\documentclass{article}
% Table captions sit above the tabular, so \belowcaptionskip is the gap between
% the caption and the top rule; the class default of 0pt makes them touch.
\setlength{\abovecaptionskip}{8pt}
\setlength{\belowcaptionskip}{5pt}
\newcommand{\papertitle}{Scheduled Re-expansion of the Opening Latent}
\title{\papertitle}
\begin{document}
\begin{abstract}
Streaming generators produce video chunk by chunk. Each chunk conditions on
generated content. Errors therefore accumulate. Systems pin a reference to
slow this. Keeping that reference is unsolved. Frozen references go stale and
refreshed ones inherit drift. We keep the opening latent and re-expand it on a
schedule. The method is training-free. It adds no state. On matched controls
it retains identity better than frozen and refreshed references. The gain is
confined to identity. Total benchmark score is unchanged.
\end{abstract}
\section{Introduction}\label{sec:intro}
Background paragraph. Generators (GEN) drift. Key-value (KV) caches are bounded.

Problem paragraph. What should the pinned reference contain, and how is it kept?

Our approach paragraph, with the motivation and the contributions.
\begin{figure}[t]\centering\caption{small figure}\end{figure}
\section{Related Work}\label{sec:related}
\begin{figure}[t]\centering\caption{taxonomy}\end{figure}
One category per paragraph.
\section{Method}\label{sec:method}
\begin{figure}[t]\centering\caption{framework}\end{figure}
Let $\sigma$ denote the noise level and let $\mathbf{z}$ be the latent.
\section{Experiments}\label{sec:exp}
\begin{table}[t]\caption{Main results}\begin{tabular}{ll}a&b\end{tabular}\end{table}
\begin{table}[t]\caption{Ablation of the schedule}\begin{tabular}{ll}a&b\end{tabular}\end{table}
Table 1 carries the results.
\section{Conclusion}\label{sec:conc}
One paragraph.
\clearpage
\appendix
\begin{center}
{\Large Appendix}\\[7pt]
{\large\papertitle}
\end{center}
% Appendix floats are numbered under their own section: Table A.1, not Table 3.
\setcounter{figure}{0}\setcounter{table}{0}
\counterwithin{figure}{section}
\counterwithin{table}{section}
\section{Extra protocol detail}\label{app:extra}
\begin{table}[t]\caption{Protocol}\begin{tabular}{ll}a&b\end{tabular}\end{table}
One paragraph.
\end{document}

\documentclass{article}
\begin{document}
\begin{abstract}
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long. Long.
Long. Long. Long. Long. Long. Long. Long.
\end{abstract}
\section{Introduction}\label{sec:intro}
Only one paragraph, and the KV cache is never defined, and a script code/run.py --fast is named.
\section{Method}\label{sec:method}
The same sentence about the pinned reference that holds the opening state is written here first.
\begin{table}[t]\caption{A caption that will rest on the top rule}\begin{tabular}{ll}a&b\end{tabular}\end{table}
\section{Experiments}\label{sec:exp}
The same sentence about the pinned reference that holds the opening state is written here again.
We report 0.812 identity in prose only.
\section{Extra}\label{sec:extra}
A section the outline never planned.
\appendix
\section{Straight on from the references, with no title of its own}\label{app:none}
\begin{table}[t]\caption{Numbered straight on from the body}\begin{tabular}{ll}a&b\end{tabular}\end{table}
One paragraph.
\end{document}
